# This repository drives orderer behavior primarily through environment variables
# in docker/docker-compose-orderer.yaml.
#
# Keep this file as a documented placeholder rather than an empty tracked file.
# If you decide to switch to mounted orderer.yaml-based configuration, replace
# this comment block with the audited orderer configuration and version it
# alongside the compose changes.
